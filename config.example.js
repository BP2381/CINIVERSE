// config.example.js
// Copy this file to config.js and add your real key
// config.js is in .gitignore so it will never be committed

const CONFIG = {
  // For local development only
  // On Netlify, the backend functions handle the key securely
  TMDB_KEY: 'YOUR_TMDB_KEY_HERE'
};